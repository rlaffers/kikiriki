#!/bin/bash
#
# Speech-to-Text Push-to-Talk Setup
# Installs dependencies and configures the system
#

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
INSTALL_DIR="${HOME}/.local/share/kikiriki"
CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/kikiriki"

echo "=========================================="
echo "  Kikiriki Speech-to-Text Push-to-Talk Setup"
echo "=========================================="
echo

# Check if running on Ubuntu/Debian
if ! command -v apt &> /dev/null; then
    echo "⚠️  Warning: This script is designed for Ubuntu/Debian systems"
    read -p "Continue anyway? (y/N) " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        exit 1
    fi
fi

# Check if running on X server
echo "Checking for X server..."
if [[ "${XDG_SESSION_TYPE:-}" != "x11" ]]; then
    if [[ -n "${XDG_SESSION_TYPE:-}" ]]; then
        echo "❌ Error: Not running on X11 (detected: ${XDG_SESSION_TYPE})"
    elif [[ -z "${DISPLAY:-}" ]]; then
        echo "❌ Error: Not running on X server (no X11 session detected)"
    else
        echo "⚠️  Warning: Cannot determine session type, but DISPLAY is set"
        echo "   Kikiriki requires X11 to function properly"
        read -p "Continue anyway? (y/N) " -n 1 -r
        echo
        if [[ ! $REPLY =~ ^[Yy]$ ]]; then
            exit 1
        fi
    fi
    
    if [[ "${XDG_SESSION_TYPE:-}" == "wayland" ]]; then
        echo "   Kikiriki requires X11 and does not support Wayland"
        exit 1
    elif [[ -n "${XDG_SESSION_TYPE:-}" && "${XDG_SESSION_TYPE}" != "x11" ]]; then
        echo "   Kikiriki requires X11 to function properly"
        exit 1
    fi
else
    echo "✅ X11 session detected"
fi
echo

# Check for whisper.cpp installation
echo "Checking for whisper.cpp..."
WHISPER_CPP_DIR="${HOME}/.local/share/whisper.cpp"
WHISPER_BIN_PATH="${WHISPER_CPP_DIR}/build/bin/whisper-cli"
WHISPER_MODEL_PATH="${WHISPER_CPP_DIR}/models/ggml-base.en.bin"
CUSTOM_WHISPER_DIR=""

if [[ -f "$WHISPER_BIN_PATH" ]]; then
    echo "✅ Found whisper-cli at default location: $WHISPER_BIN_PATH"
    
    if [[ -f "$WHISPER_MODEL_PATH" ]]; then
        echo "✅ Found Whisper model at: $WHISPER_MODEL_PATH"
    else
        echo "⚠️  Warning: Whisper model not found at ${WHISPER_MODEL_PATH}"
        echo "   You may need to download a model using:"
        echo "   ${WHISPER_CPP_DIR}/models/download-ggml-model.sh base.en"
    fi
else
    echo "⚠️  whisper-cli not found at default location: $WHISPER_BIN_PATH"
    echo
    
    while true; do
        read -p "Enter the path to your whisper.cpp installation directory (or press Enter to skip): " -r CUSTOM_WHISPER_DIR
        
        # User chose to skip
        if [[ -z "$CUSTOM_WHISPER_DIR" ]]; then
            echo "⚠️  Skipping whisper.cpp configuration"
            echo "   You'll need to edit the paths in: ${CONFIG_DIR}/kikiriki.conf"
            break
        fi
        
        # Expand ~ to home directory
        CUSTOM_WHISPER_DIR="${CUSTOM_WHISPER_DIR/#\~/$HOME}"
        
        # Check if the binary exists at the custom location
        CUSTOM_BIN="${CUSTOM_WHISPER_DIR}/build/bin/whisper-cli"
        if [[ -f "$CUSTOM_BIN" ]]; then
            echo "✅ Found whisper-cli at: $CUSTOM_BIN"
            WHISPER_BIN_PATH="$CUSTOM_BIN"
            
            # Check for model at custom location
            CUSTOM_MODEL="${CUSTOM_WHISPER_DIR}/models/ggml-base.en.bin"
            if [[ -f "$CUSTOM_MODEL" ]]; then
                echo "✅ Found Whisper model at: $CUSTOM_MODEL"
                WHISPER_MODEL_PATH="$CUSTOM_MODEL"
            else
                echo "⚠️  Warning: Whisper model not found at ${CUSTOM_MODEL}"
                echo "   You may need to download a model using:"
                echo "   ${CUSTOM_WHISPER_DIR}/models/download-ggml-model.sh base.en"
                WHISPER_MODEL_PATH="$CUSTOM_MODEL"
            fi
            break
        else
            echo "❌ Error: whisper-cli not found at: $CUSTOM_BIN"
            echo "   Please check the path and try again"
            echo
        fi
    done
fi

echo
echo "Installing system dependencies..."

# Check which packages need to be installed
PACKAGES_TO_INSTALL=()
for pkg in xbindkeys alsa-utils pulseaudio-utils x11-xserver-utils xdotool xclip libnotify-bin ffmpeg; do
    if ! dpkg-query -W -f='${Status}' "$pkg" 2>/dev/null | grep -q "install ok installed"; then
        PACKAGES_TO_INSTALL+=("$pkg")
    fi
done

if [[ ${#PACKAGES_TO_INSTALL[@]} -gt 0 ]]; then
    echo "Will install: ${PACKAGES_TO_INSTALL[*]}"
    sudo apt update
    sudo apt install -y "${PACKAGES_TO_INSTALL[@]}"
    echo "✅ Dependencies installed"
else
    echo "✅ All dependencies already installed"
fi

# Create installation directory
echo
echo "Creating installation directory..."
mkdir -p "${INSTALL_DIR}/sounds"
echo "✅ Created: ${INSTALL_DIR}"

# Copy runtime files
echo
echo "Copying files to installation directory..."
cp "${SCRIPT_DIR}/kikiriki" "${INSTALL_DIR}/"
cp "${SCRIPT_DIR}/kikiriki-bind-keys" "${INSTALL_DIR}/"
cp "${SCRIPT_DIR}/kikiriki-unbind-keys" "${INSTALL_DIR}/"
cp "${SCRIPT_DIR}/keyhold-handler" "${INSTALL_DIR}/"
cp "${SCRIPT_DIR}/uninstall" "${INSTALL_DIR}/"
cp "${SCRIPT_DIR}/sounds/message.oga" "${INSTALL_DIR}/sounds/"
cp "${SCRIPT_DIR}/sounds/complete.oga" "${INSTALL_DIR}/sounds/"
echo "✅ Files copied"

# Make scripts executable
echo
echo "Making scripts executable..."
chmod +x "${INSTALL_DIR}/kikiriki"
chmod +x "${INSTALL_DIR}/kikiriki-bind-keys"
chmod +x "${INSTALL_DIR}/kikiriki-unbind-keys"
chmod +x "${INSTALL_DIR}/keyhold-handler"
chmod +x "${INSTALL_DIR}/uninstall"
echo "✅ Scripts are executable"

# Create configuration directory and copy default config
echo
echo "Setting up configuration..."
mkdir -p "$CONFIG_DIR"

if [[ -f "${CONFIG_DIR}/kikiriki.conf" ]]; then
    echo "⚠️  Configuration file already exists: ${CONFIG_DIR}/kikiriki.conf"
    echo "   Keeping existing configuration (not overwriting)"
else
    cp "${SCRIPT_DIR}/kikiriki.conf" "${CONFIG_DIR}/"
    
    # If custom whisper paths were detected or provided, update the config
    if [[ -n "$CUSTOM_WHISPER_DIR" ]]; then
        sed -i "s|WHISPER_MODEL=.*|WHISPER_MODEL=\"${WHISPER_MODEL_PATH}\"|" "${CONFIG_DIR}/kikiriki.conf"
        sed -i "s|WHISPER_BIN=.*|WHISPER_BIN=\"${WHISPER_BIN_PATH}\"|" "${CONFIG_DIR}/kikiriki.conf"
        echo "✅ Created configuration with custom whisper.cpp paths: ${CONFIG_DIR}/kikiriki.conf"
    else
        echo "✅ Created default configuration: ${CONFIG_DIR}/kikiriki.conf"
    fi
fi

# Setup xbindkeys configuration
echo
echo "Generating xbindkeys configuration..."

XBINDKEYS_CONFIG="${CONFIG_DIR}/kikiriki-xbindkeys.conf"
XBINDKEYS_TEMPLATE="${SCRIPT_DIR}/kikiriki-xbindkeys.conf.template"

# Check if template exists
if [[ ! -f "$XBINDKEYS_TEMPLATE" ]]; then
    echo "❌ Error: xbindkeys template not found: $XBINDKEYS_TEMPLATE"
    exit 1
fi

# Generate kikiriki-xbindkeys.conf from template
if [[ -f "$XBINDKEYS_CONFIG" ]]; then
    echo "⚠️  xbindkeys config already exists: ${XBINDKEYS_CONFIG}"
    echo "   Keeping existing configuration (not overwriting)"
else
    sed "s|KIKIRIKI_INSTALL_DIR|${INSTALL_DIR}|g" "$XBINDKEYS_TEMPLATE" > "$XBINDKEYS_CONFIG"
    echo "✅ Generated xbindkeys configuration: $XBINDKEYS_CONFIG"
fi

# Setup systemd user service
echo
echo "Setting up systemd user service..."

SYSTEMD_USER_DIR="${HOME}/.config/systemd/user"
SERVICE_FILE="${SYSTEMD_USER_DIR}/kikiriki.service"
SERVICE_TEMPLATE="${SCRIPT_DIR}/kikiriki.service"

mkdir -p "$SYSTEMD_USER_DIR"

# Check if service template exists
if [[ ! -f "$SERVICE_TEMPLATE" ]]; then
    echo "❌ Error: Service template not found: $SERVICE_TEMPLATE"
    exit 1
fi

# Copy and customize the service file
sed "s|KIKIRIKI_INSTALL_DIR|${INSTALL_DIR}|g" "$SERVICE_TEMPLATE" > "$SERVICE_FILE"

# Reload systemd user daemon
systemctl --user daemon-reload

# Enable the service to start on login
systemctl --user enable kikiriki.service
echo "✅ Created and enabled systemd user service"

# Optionally start the service now
echo
read -p "Start the service now? (Y/n) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Nn]$ ]]; then
    systemctl --user start kikiriki.service
    sleep 1
    echo
    systemctl --user status kikiriki.service --no-pager
fi

echo
echo "=========================================="
echo "  ✅ Setup Complete!"
echo "=========================================="
echo
echo "📁 Installation directory: ${INSTALL_DIR}"
echo "⚙️  Configuration file: ${CONFIG_DIR}/kikiriki.conf"
echo "🔧 xbindkeys config: ${CONFIG_DIR}/kikiriki-xbindkeys.conf"
echo
echo "🎯 Default hotkey: Ctrl+Space"
echo "   - Press and hold to record"
echo "   - Release to transcribe"
echo
echo "To customize settings, edit:"
echo "  ${CONFIG_DIR}/kikiriki.conf"
echo
echo "To change the hotkey, edit:"
echo "  ${CONFIG_DIR}/kikiriki-xbindkeys.conf"
echo "  Then run: systemctl --user restart kikiriki.service"
echo
echo "Service Management:"
echo "  Start:   systemctl --user start kikiriki.service"
echo "  Stop:    systemctl --user stop kikiriki.service"
echo "  Restart: systemctl --user restart kikiriki.service"
echo "  Status:  systemctl --user status kikiriki.service"
echo "  Logs:    journalctl --user -u kikiriki.service -f"
echo
echo "Auto-start on login:"
echo "  Enable:  systemctl --user enable kikiriki.service"
echo "  Disable: systemctl --user disable kikiriki.service"
echo
echo "To uninstall:"
echo "  ${INSTALL_DIR}/uninstall"
echo
echo "To test the script manually:"
echo "  ${INSTALL_DIR}/kikiriki start"
echo "  # speak something"
echo "  ${INSTALL_DIR}/kikiriki stop"
echo
